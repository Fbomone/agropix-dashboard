# Dependencias directas del dashboard Agropix (UTF-8, sin BOM).
# Se usan pisos de version en vez de pines exactos: el entorno local corre
# Python 3.14 y Streamlit Cloud puede resolver otra version del interprete.
# pip resuelve las transitivas (altair, pyarrow, requests, google-auth-*, etc.).

# --- App ---
streamlit>=1.63.0
pandas>=2.2
numpy>=1.26
plotly>=6.0

# --- Google Sheets ---
gspread>=6.2.1
google-auth>=2.58.0

# --- Exportacion PDF ---
reportlab>=4.2
pillow>=10.4
kaleido>=1.4.0         # render de graficos Plotly a imagen (necesita Chromium, ver packages.txt)

# --- Config / email ---
python-dotenv>=1.0
sendgrid>=6.10         # envio automatico de reportes (plan gratis: 100 mails/dia)
