venv/
__pycache__/
*.pyc
.pytest_cache/
.env
credentials.json
data/cache/
data/auditoria.log

# Secretos: el tema (.streamlit/config.toml) SI se versiona para que viaje al
# deploy; las credenciales NO.
.streamlit/secrets.toml

# Ningun .json nuevo entra al repo por descuido (credenciales, exports, dumps).
*.json
# Excepto los que el proyecto necesita. Los archivos ya versionados no se
# desversionan por la regla de arriba, pero sin estas excepciones un rm + add
# los perderia en silencio.
!precios_lista.json
!.devcontainer/devcontainer.json
salida/
data/envios.jsonl
